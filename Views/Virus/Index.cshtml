@model IEnumerable<LaboratorioVacinas.Models.Virus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TemVacina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TemVacina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VirusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VirusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VirusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
